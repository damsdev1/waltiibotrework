// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Config {
  key   String @id @unique
  value String
}

model User {
  id           String        @id @unique
  accessToken  String
  refreshToken String
  tokenExpiry  DateTime
  Connections  Connections[]
}

model Connections {
  id         Int    @id @default(autoincrement())
  userId     String
  platform   String
  platformId String
  user       User   @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([platform, platformId], name: "PlatformPlatformId") // prevents same connection being assigned to multiple users
}

model Giveaway {
  id            Int             @id @default(autoincrement())
  interactionId String          @unique
  channelId     String?
  messageId     String?         @unique
  prize         String
  endTime       DateTime
  ended         Boolean         @default(false)
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt
  subOnly       Boolean         @default(false)
  winnerUserId  String?
  GiveawayEntry GiveawayEntry[]
}

model GiveawayEntry {
  id         Int      @id @default(autoincrement())
  giveawayId Int
  userId     String
  chances    Int      @default(1)
  joinedAt   DateTime @default(now())
  giveaway   Giveaway @relation(fields: [giveawayId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([giveawayId, userId], name: "GiveawayUser") // prevents same user entering giveaway multiple times
}
